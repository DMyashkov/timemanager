DJANGO_SECRET_KEY = 'django-insecure-@wd7db*7=egw83rw^x(e_z^u!=$d7+#8@+cwd%e3jvn9m=pkld'
DJANGO_DEBUG = 'True'

# Database Name
POSTGRES_DB=timemanager_db

# Database User
POSTGRES_USER=myuser

# Database Password
POSTGRES_PASSWORD=mypassword

# Database Host (for Docker Compose, this is the service name of your PostgreSQL container)
POSTGRES_HOST=postgres-db

# Database Port (default for PostgreSQL is 5432)
POSTGRES_PORT=5432

# ---------------------------------
# 🌐 CORS Configuration 🌐
# ---------------------------------

# CORS Allowed Origins (for React Native, etc.)
CORS_ALLOWED_ORIGINS=http://localhost:19006,http://localhost:8000,http://127.0.0.1:8081

# CORS Allow Credentials (True/False)
CORS_ALLOW_CREDENTIALS=True


